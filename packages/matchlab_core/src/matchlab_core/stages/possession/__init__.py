"""Possession-transition spotting stages (SPO-77+).

Per-frame ball-possessor estimation feeding the transition->event rules.
`none` is the disabled stub; `possession-heuristic-image` (SPO-79) is the
Phase 1 baseline; `possession-peral` (SPO-83 gate) is the learned follow-up.
"""
